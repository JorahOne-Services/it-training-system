name: learning-path-engine
description: Build personalized training plans from skill gaps.
---

## Steps
1. Required skills vs `user_skills`.
2. Query relevant `videos` by skill tags.
3. Create `learning_paths` + ordered `learning_path_items`.
4. Notify Telegram.
