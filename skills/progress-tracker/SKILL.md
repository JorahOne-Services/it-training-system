name: progress-tracker
description: Track user progress, emit reports, and trigger reminders.
---

## Steps
1. Pull latest `quiz_attempts`.
2. Evaluate `learning_path_items`.
3. Send reminders via `/training/notify`.
